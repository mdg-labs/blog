/**
 * Copy-paste snippet for consumer `src/content.config.ts` (or `content.config.ts` on PipeWatch).
 *
 * Merge the `blog` collection with your existing collections (changelog, legal, etc.).
 * Adjust `contentBase` and `locales` per site — see templates/README.md.
 */
// @ts-nocheck — reference only; validated when pasted into a consumer Astro project.

/*
import { defineCollection } from "astro:content";
import { defineBlogCollection } from "@mdg-labs/blog";

// --- website / slugbase marketing ---
const blog = defineBlogCollection({
  contentBase: "./src/content/blog",
  locales: ["en", "de"],
  // defaultAuthor: "MDG Labs", // optional site-wide default
});

// --- pipewatch marketing (en only) ---
// const blog = defineBlogCollection({
//   contentBase: "./content/blog",
//   locales: ["en"],
// });

export const collections = {
  blog,
  // changelog,
  // legal,
};
*/
